services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - budget_data:/app/data
    environment:
      PORT: 3001
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
    restart: unless-stopped

volumes:
  budget_data:
