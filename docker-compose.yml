services:
  app:
    build: .
    ports:
      - "6000:6000"
    volumes:
      - budget_data:/app/data
    environment:
      PORT: 6000
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
    restart: unless-stopped

volumes:
  budget_data:
